// Shared sizing for chat bar (slightly slimmer for a tighter footprint)
export const CHAT_BAR_MAX_W = 760;
export const CHAT_BAR_VW = 0.78;

// Wider content column for dashboards/pages
// Align dashboard content width to the chat bar footprint for consistent edges
export const CHAT_CONTENT_MAX_W = 860;
export const CHAT_CONTENT_VW = 0.82;

// Internal padding baked into ChatCanvas width calc; leave at 0 now that we trim width directly
export const CHAT_ALIGN_NUDGE_PX = 0;
